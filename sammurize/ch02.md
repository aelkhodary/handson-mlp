# Chapter 2: End-to-End Machine Learning Project

---

# 📌 What this document is about

This is a **complete roadmap for building an AI system from scratch**. It uses a real example: predicting house prices in California based on neighborhood data (like income, population, and location).

The main message is: **Building AI is 80% preparation and 20% actual "magic."** Most of the work is cleaning data, organizing it, and making sure your system keeps working after you launch it.

---

## Overview

This chapter walks through a complete ML project from start to finish using the **California Housing Prices** dataset. The goal is to predict median housing prices in districts based on census data.

---

## The 8 Main Steps of an ML Project

1. **Look at the big picture** - Understand business objectives
2. **Get the data** - Download and load the dataset
3. **Explore and visualize** - Gain insights from the data
4. **Prepare the data** - Clean and transform for ML algorithms
5. **Select and train a model** - Try different algorithms
6. **Fine-tune the model** - Optimize hyperparameters
7. **Present your solution** - Communicate results effectively
8. **Launch, monitor, and maintain** - Deploy and keep the system running

---

## 1. Look at the Big Picture

### Frame the Problem
- **Ask the right questions:** What is the business objective? How will the model be used?
- **Understand the current solution:** This gives a performance baseline and insights
- **Identify the ML task type:**
  - **Supervised learning** (labeled data with housing prices)
  - **Regression task** (predicting a continuous value)
  - **Multiple regression** (using multiple features)
  - **Univariate regression** (predicting single value per instance)
  - **Batch learning** (data fits in memory, no need for online learning)

### Select a Performance Measure
- **RMSE (Root Mean Squared Error):** Preferred for regression; penalizes large errors more
- **MAE (Mean Absolute Error):** Better when outliers are common; less sensitive to large errors
- **Key insight:** Higher norm indices (like l2 for RMSE) focus more on large values

### Check Assumptions
- Verify assumptions with downstream teams early
- Example: If prices are converted to categories downstream, it's a classification problem, not regression

---

## 2. Get the Data

### Best Practices
- **Automate data fetching:** Write functions to download data programmatically
- **Use version control for data:** Ensure reproducibility
- **Document data sources:** Know where data comes from

### Quick Data Exploration
- housing.head() - View first rows
- housing.info() - Data types, non-null counts
- housing.describe() - Statistical summary
- housing["column"].value_counts() - Category distribution
- housing.hist(bins=50) - Visualize distributions

### Key Observations from Histograms
- Some features are **capped** (e.g., median income, house values)
- Features have **different scales** - need scaling later
- Many distributions are **skewed right** - may need transformation

---

## 3. Create a Test Set

### Why Create Test Set Early?
- **Avoid data snooping bias:** Your brain is prone to overfitting
- Looking at test data influences model selection subconsciously
- Keep test set aside until final evaluation

### Methods for Splitting
1. **Random split:** Simple but changes each run
2. **Fixed seed:** Use random_state=42 for reproducibility
3. **Hash-based split:** Stable even when data updates (use unique identifiers)

### Stratified Sampling
- **Problem:** Random sampling may not represent the population
- **Solution:** Ensure test set has same proportions as full dataset for important features
- **Example:** Stratify by income category to maintain representative distribution

---

## 4. Explore and Visualize Data

### Visualizing Geographical Data
- Use scatter plots with alpha for density visualization
- Color-code by target variable (house prices)
- Size points by another feature (population)

### Look for Correlations
**Key insight:** Median income is the strongest predictor of housing prices (0.69 correlation)

### Correlation Limitations
- Correlation coefficient only measures **linear relationships**
- Can completely miss **nonlinear relationships**
- Always visualize data, don't rely solely on statistics

### Experiment with Attribute Combinations
Create new features that might be more predictive:
- rooms_per_house = total_rooms / households
- bedrooms_ratio = total_bedrooms / total_rooms
- people_per_house = population / households

**Result:** bedrooms_ratio has strong negative correlation (-0.26) with price

---

## 5. Prepare the Data

### Why Write Functions for Data Preparation?
- **Reproducibility:** Apply same transformations to new data
- **Reusability:** Build a library for future projects
- **Experimentation:** Easily try different transformations

### Handle Missing Values (3 Options)
1. **Drop rows** with missing values
2. **Drop the entire attribute**
3. **Impute** with mean, median, or most frequent value

### Handle Categorical Attributes

**Ordinal Encoding:** For ordered categories

**One-Hot Encoding:** For unordered categories (preferred)

**Why OneHotEncoder over pd.get_dummies()?**
- Remembers categories from training
- Handles unknown categories gracefully
- Ensures consistent features in production

### Feature Scaling

Feature scaling is a technique to **normalize the range of features** so that they all have similar scales. This is critical because many ML algorithms perform poorly when features have very different scales.

**Why is Feature Scaling Important?**

Example from the California Housing dataset:
- `total_rooms`: ranges from 6 to 39,320
- `median_income`: ranges from 0 to 15

Without scaling, most models will **ignore median income** and focus on total_rooms simply because its values are much larger — even though median income is actually the strongest predictor of housing prices!

---

**Min-Max Scaling (Normalization):**

Formula: `X_scaled = (X - X_min) / (X_max - X_min)`

- Transforms all values to fall between **0 and 1**
- The minimum value becomes 0, maximum becomes 1

Example:
```
Original: [10, 20, 30, 40, 50]  →  Scaled: [0.0, 0.25, 0.5, 0.75, 1.0]
```

**Problem with outliers:**
```
Original: [10, 20, 30, 40, 1000]  ← 1000 is an outlier
Scaled:   [0.0, 0.01, 0.02, 0.03, 1.0]  ← All values crushed near 0!
```

---

**Standardization (Z-score Normalization):**

Formula: `X_scaled = (X - mean) / standard_deviation`

- Centers data around **mean = 0**
- Scales so that **standard deviation = 1**
- Values are NOT bounded to a specific range

Example:
```
Original: [10, 20, 30, 40, 50]  →  Scaled: [-1.41, -0.71, 0.0, 0.71, 1.41]
```

**With outliers (more robust):**
```
Original: [10, 20, 30, 40, 1000]
Scaled:   [-0.53, -0.51, -0.48, -0.46, 1.98]  ← Values not crushed!
```

---

**When to Use Which?**

| Scaling Method | Use When | Avoid When |
|----------------|----------|------------|
| **Min-Max** | Need bounded values (0-1), Neural networks | Data has outliers |
| **Standardization** | Data has outliers, Most ML algorithms | Need bounded range |

**Code Example:**
```python
from sklearn.preprocessing import MinMaxScaler, StandardScaler

# Min-Max Scaling
min_max = MinMaxScaler()
data_scaled = min_max.fit_transform(data)

# Standardization (preferred)
standard = StandardScaler()
data_scaled = standard.fit_transform(data)
```

> **Key Takeaway:** Standardization is preferred in most cases because it's more robust to outliers and works well with most ML algorithms.

### Handle Heavy-Tailed Distributions
- **Log transformation:** For power-law distributions
- **Square root:** For moderately skewed data
- **Bucketizing:** Divide into equal-sized buckets

### Transformation Pipelines

**Pipeline:** Chain multiple transformers

**ColumnTransformer:** Apply different pipelines to different columns

---

## 6. Select and Train a Model

### Start Simple
Begin with LinearRegression, then try more complex models

### Identify Problems
- **Underfitting:** Model too simple, high training error
  - Try more powerful model
  - Add better features
  - Reduce regularization
  
- **Overfitting:** Model too complex, low training error but high validation error
  - Simplify model
  - Add regularization
  - Get more training data

### Cross-Validation
**Benefits:**
- Get performance estimate AND precision (standard deviation)
- More reliable than single validation set
- **Cost:** Trains model multiple times

### Model Comparison Results
| Model | RMSE | Notes |
|-------|------|-------|
| Linear Regression | ~70,000 | Underfitting |
| Decision Tree | ~66,500 | Overfitting (0 training error) |
| Random Forest | ~47,000 | Best performer, some overfitting |

---

## 7. Fine-Tune Your Model

### Grid Search
Systematically try all combinations of hyperparameters

### Randomized Search
Better for large hyperparameter spaces

**Advantages of Randomized Search:**
- Explores more values for continuous hyperparameters
- Unaffected by irrelevant hyperparameters
- Fixed number of iterations regardless of search space size

### Analyze Feature Importances
Use this to:
- Drop unimportant features
- Understand what the model learned
- Guide feature engineering

### Evaluate on Test Set
- Compute final RMSE
- Calculate confidence interval
- Compare with baseline

---

## 8. Launch, Monitor, and Maintain

### Deployment Options
1. **Simple:** Load model in web application
2. **Web service:** REST API for predictions (better for scaling)
3. **Cloud platforms:** Google Vertex AI, AWS SageMaker, etc.

### Monitoring is Critical
- **Track live performance:** Set up alerts for degradation
- **Watch for data drift:** Model trained on old data may not work on new patterns
- **Use downstream metrics:** e.g., sales of recommended products
- **Human evaluation:** Sample predictions for expert review

### Automate Retraining
- Collect fresh data regularly
- Script automatic training and hyperparameter tuning
- Compare new model vs. old model before deployment
- Monitor input data quality

### Best Practices
- **Keep backups** of all models and datasets
- **Enable rollback** to previous versions
- **Document everything** for reproducibility
- **Test on subgroups** (fairness analysis)

---

## Scikit-Learn Design Principles

### Key Concepts
- **Estimators:** Objects that learn from data (fit() method)
- **Transformers:** Estimators that transform data (transform() method)
- **Predictors:** Estimators that make predictions (predict() method)

### Design Philosophy
- **Consistency:** Simple, uniform API across all estimators
- **Inspection:** Hyperparameters via public attributes, learned params with _ suffix
- **Nonproliferation:** Use NumPy arrays and SciPy matrices, not custom classes
- **Composition:** Build complex pipelines from simple building blocks
- **Sensible defaults:** Quick baseline with minimal configuration

---

## Key Takeaways

1. **Process matters:** Much of ML work is data preparation, not algorithm selection
2. **Start simple:** Begin with basic models, add complexity as needed
3. **Validate properly:** Use stratified sampling and cross-validation
4. **Automate everything:** Data fetching, preprocessing, training, deployment
5. **Monitor continuously:** Models degrade over time due to data drift
6. **Document and version:** Keep track of datasets, models, and experiments
7. **Check fairness:** Ensure model works well across all subgroups

---

## Exercises Summary

1. Try SVR with different kernels and hyperparameters
2. Replace GridSearchCV with RandomizedSearchCV
3. Add SelectFromModel for automatic feature selection
4. Create custom transformer using KNN for smoothed median income
5. Explore preparation options with RandomizedSearchCV
6. Implement StandardScalerClone with full API support
7. Apply the complete process to a new dataset (Vehicle or Bike Sharing)

---

# 🔍 Beginner-Friendly Step-by-Step Explanation

## **Step 1: Understand What You're Building (Frame the Problem)**

Before writing any code, ask:
- **What's the goal?** → Predict house prices
- **Who will use it?** → Another system that decides where to invest
- **What exists today?** → Human experts who are often wrong by 30%

**Key terms:**
- **Supervised learning** = You have examples with correct answers (house prices)
- **Regression** = Predicting a number (not a category like "cheap" or "expensive")

---

## **Step 2: Get and Explore the Data**

You download a spreadsheet with 20,000 neighborhoods. Each row has info like:
- Location (latitude, longitude)
- Population
- Average income
- Average house price

**What you look for:**
- Missing information (some neighborhoods don't have bedroom counts)
- Weird patterns (some prices are "capped" at $500,000)
- Different scales (income is 0-15, but rooms can be 6-39,000)

---

## **Step 3: Split the Data (The "Pop Quiz" Rule)**

**This is critical:** Before doing anything else, hide 20% of your data in a "secret box" (called the **Test Set**).

**Why?** Your brain is sneaky. If you see all the data, you'll unconsciously pick a model that fits it perfectly but fails on new data. This is called **data snooping**.

**Stratified sampling** = Make sure your hidden data has the same mix of rich/poor neighborhoods as the full data. Otherwise, your test is unfair.

---

## **Step 4: Look for Patterns (Explore)**

You make charts and calculate **correlations** (how much one thing relates to another).

**Findings:**
- **Median income** is the #1 predictor of house prices (correlation: 0.69)
- Location near the ocean matters
- Raw data like "total rooms" isn't useful alone, but "rooms per house" is better

**Key insight:** You can **create new features** by combining existing ones:
- `bedrooms_ratio = total_bedrooms / total_rooms`
- This new feature is MORE useful than the original ones!

---

## **Step 5: Clean and Prepare the Data**

Computers are picky. You must:

1. **Fill in missing values (Imputation)**
   - Replace blanks with the average or most common value

2. **Convert words to numbers (Encoding)**
   - "NEAR OCEAN" → `[0, 0, 0, 1, 0]` (One-Hot Encoding)
   - This creates a separate column for each category

3. **Make all numbers the same size (Scaling)**
   - **Min-Max Scaling:** Squish everything to 0-1
   - **Standardization:** Make the average = 0, spread = 1 (preferred because it handles weird values better)

4. **Build a Pipeline**
   - A **pipeline** is like an assembly line: data goes in one end, gets cleaned step-by-step, and comes out ready for the model

---

## **Step 6: Train Different Models**

You try several "brains":

| Model | Error (RMSE) | Problem |
|-------|--------------|---------|
| Linear Regression | $70,000 | Too simple (**underfitting**) |
| Decision Tree | $0 on training, $66,500 on test | Memorized answers (**overfitting**) |
| Random Forest | $47,000 | Best! Still some overfitting |

**Cross-validation** = Instead of one test, you do 10 mini-tests and average the results. This gives you a more reliable score.

---

## **Step 7: Fine-Tune the Best Model**

Models have "knobs" called **hyperparameters**. You try different settings:

- **Grid Search:** Try every combination (slow but thorough)
- **Randomized Search:** Try random combinations (faster, often just as good)

Example: The Random Forest has a knob called `max_features`. Testing values 4, 6, 8 finds that 6 works best.

---

## **Step 8: Launch and Monitor**

Deployment options:
- Save the model as a file and load it in your app
- Create a web service (REST API) that other apps can call
- Use cloud services like Google Vertex AI

**Critical:** Your model will get worse over time because the world changes (**data drift**). You must:
- Monitor performance regularly
- Retrain with fresh data
- Keep backups so you can roll back if something breaks

---

# 🔢 Simple Example: Teaching a Robot to Guess Bicycle Prices

1. **Get data:** 100 bikes with age, brand, and price
2. **Hide 20 bikes** in a closet (test set)
3. **Explore:** Notice older bikes are cheaper
4. **Clean:** One bike is missing its age → guess it's 5 years old
5. **Scale:** Age (1-10 years) and price ($50-$500) are on different scales → standardize both
6. **Train:** Try 3 different models, pick the best one
7. **Tune:** Adjust the model's settings to improve accuracy
8. **Test:** Show the robot the 20 hidden bikes → it guesses within $30? Success!
9. **Launch:** Put the robot in a bike shop
10. **Monitor:** Check every month if it's still accurate

---

# 🧠 Key Ideas to Remember

1. **"Garbage in, garbage out"** → Clean data is more important than fancy algorithms

2. **Always hide a test set FIRST** → Never peek until the very end

3. **Feature engineering is powerful** → Combining existing data (like `rooms_per_house`) often beats collecting new data

4. **Simple models first** → Start with Linear Regression, then try complex ones

5. **Cross-validation is your friend** → One test can be lucky; 10 tests give you the truth

6. **Overfitting is the enemy** → A model that scores 100% on training data is usually useless in real life

7. **Deployment is just the beginning** → Monitoring and maintenance take more work than building the model

8. **Pipelines save your life** → Automate everything so you can reproduce results and update easily

---

# 📋 Quick Reference: The 8-Step ML Recipe

```
1. Frame the problem → What are we predicting? Who uses it?
2. Get data → Download, explore, understand
3. Split data → Hide 20% for final testing
4. Explore → Find patterns, correlations, create new features
5. Prepare → Clean missing values, encode text, scale numbers
6. Train → Try multiple models, use cross-validation
7. Tune → Adjust hyperparameters with Grid/Random Search
8. Launch → Deploy, monitor, retrain, repeat
```
